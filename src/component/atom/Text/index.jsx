const TextLabel = (props) => {
    const {label} = props;
    return (
     
    <span className="font-inter font-normal text-xs leading-22 tracking-tighter text-center text-black">{label}</span>
  )
}

export default TextLabel